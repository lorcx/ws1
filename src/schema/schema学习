schema规范
1.所有的标签和属性都需要有人定义
2.所有的schema文件都需要一个id(namespace)
3.namespace由谁来指定？
    有targetNamespace来指定，它的值有可能并不存在
4.如何引入一个schema约束？
    xmlns属性
5.如果引入的schema不是w3c组织的自己写的，必须指定schema文件的位置
6. xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"用来引入别的w3c标签
    xsi是引入的别名
    若有有多个xmlns引入约束就要设置别名
7.xmlns:tt="http://lx.com"
  引入的定义的所有自定义标签则需要在所有标签上加上别名